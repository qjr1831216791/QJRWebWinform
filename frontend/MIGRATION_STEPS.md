# 迁移步骤说明

## ✅ 已完成的工作

1. ✅ 更新了 `package.json`，添加了所有依赖
2. ✅ 修改了 `main.js`，整合了 VueWebProj 的依赖和 NativeHost 等待逻辑
3. ✅ 更新了 `App.vue`，使用 router-view
4. ✅ 创建了路由配置文件 `src/router/index.js`（使用 hash 模式）
5. ✅ 复制了 `public/config/WebConfig.js`

## 📋 需要手动完成的工作

由于文件较大且路径包含中文，以下文件需要手动复制：

### 1. 复制 public/js 目录下的文件

从 `VueWebProj/vue-web/public/js/` 复制到 `QJRWebWinform/frontend/public/js/`：

- `JsCrmHelper.js`
- `rtcrm.min.js`
- `ToLunar.js`

### 2. 复制 src 目录

从 `VueWebProj/vue-web/src/` 复制以下内容到 `QJRWebWinform/frontend/src/`：

- `src/components/` - 所有组件目录
- `src/assets/` - 静态资源（图片等）

**注意**：不需要复制 `src/main.js` 和 `src/App.vue`，因为已经修改过了。

### 3. 安装依赖

在 `frontend` 目录下运行：

```bash
npm install
```

这将安装以下依赖：
- element-ui@^2.15.14
- axios@^1.5.0
- echarts@^5.5.1
- vue-router@^3.0.1

## 🧪 测试步骤

1. **安装依赖**：
   ```bash
   cd frontend
   npm install
   ```

2. **开发模式测试**：
   ```bash
   npm run serve
   ```
   访问 http://localhost:8080 查看是否正常

3. **构建测试**：
   ```bash
   npm run build
   ```
   检查是否成功构建到 `src/QJRWebWinform.WPF/wwwroot/`

4. **在 WPF 应用中测试**：
   - 在 Visual Studio 中运行 WPF 项目
   - 检查应用是否正常加载
   - 测试各个功能模块

## ⚠️ 注意事项

1. **路由模式**：已设置为 `hash` 模式，适合桌面应用
2. **静态资源路径**：确保 `public` 目录下的文件正确复制
3. **API 调用**：如果使用外部 API，确保服务可访问
4. **NativeHost**：应用会等待 NativeHost 初始化，超时后仍会继续运行

## 🔧 如果遇到问题

1. **依赖安装失败**：检查 Node.js 版本（需要 14.x+）
2. **构建失败**：检查文件路径是否正确
3. **运行时错误**：检查浏览器控制台（F12）查看错误信息
4. **路由不工作**：确保使用 hash 模式（已配置）

## 📝 文件清单

需要复制的文件/目录：

```
VueWebProj/vue-web/
├── public/
│   └── js/
│       ├── JsCrmHelper.js
│       ├── rtcrm.min.js
│       └── ToLunar.js
└── src/
    ├── components/  (整个目录)
    └── assets/      (整个目录)
```

复制到：

```
QJRWebWinform/frontend/
├── public/
│   └── js/          (复制 js 文件到这里)
└── src/
    ├── components/  (复制组件到这里)
    └── assets/      (复制资源到这里)
```

## ✅ 迁移完成检查清单

- [ ] 复制了 public/js 目录下的所有文件
- [ ] 复制了 src/components 目录
- [ ] 复制了 src/assets 目录
- [ ] 运行了 `npm install`
- [ ] 测试了 `npm run serve`
- [ ] 测试了 `npm run build`
- [ ] 在 WPF 应用中测试了功能

